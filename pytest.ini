# pytest configuration now lives in pyproject.toml
# This file is kept for backwards compatibility with run_tests.sh
[pytest]
pythonpath = .
  custom_components/homeworks_hwi
testpaths = tests
norecursedirs = .git __pycache__
asyncio_mode = auto
markers =
    requires_ha: mark test as requiring Home Assistant
